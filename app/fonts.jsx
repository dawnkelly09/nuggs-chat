import { BioRhyme, Rubik } from "next/font/google";

export const bio = BioRhyme({
    subsets: ["latin"],
    weight: ['400', '700'],
    display: "swap",
})

export const rubik = Rubik({
    subsets: ["latin"],
    display: "swap",
})